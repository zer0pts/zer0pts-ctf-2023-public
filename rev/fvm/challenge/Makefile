all:
#	gcc main.c -masm=intel -o test
#	gcc test.S -masm=intel -o test
#	nasm test.S -fELF64
#	ld test.o -o test -lc -e main -dynamic-linker /lib/ld64.so.1
	g++ main.cpp -o ../distfiles/fvm -masm=intel -O3
